# 赫立-研发 全局业务拓扑

> 从 dependency_graph.json 自动生成。 24 表 / 68 边 / 25 工作流。
> 只读表: 9 | 只写表: 9 | 双向: 3 | 孤立: 3

---

## 核心实体角色速查

| 实体 | r/w | 角色 | 核心工作流 |
|------|-----|------|-----------|
| **研发项目** | 3/3 | 流程节点 | 人工成本新增至成本清单、每日报工跟新任务进度、项目启动 |
| **任务分解与分配** | 3/2 | 流程节点 | 人工成本新增至成本清单、任务进度-1、任务进度-2 |

---

## 各实体生命周期明细

### 研发项目 (3写/3读)

**创建/更新者**:
- 人工成本新增至成本清单 `人工成本新增至成本清单`
- 每日报工跟新任务进度 `每日报工跟新任务进度`
- 项目启动 `项目启动`

**消费者**:
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`
- 自动生成薪资明细 `自动生成薪资明细`

**下游写入**:
- → `产品/服务` (获取BOM模板)
- → `项目WBS模板` (新增WBS任务)
- → 链式影响: `员工组织职级`

### 任务分解与分配 (3写/2读)

**创建/更新者**:
- 人工成本新增至成本清单 `人工成本新增至成本清单`
- 任务进度-1 `任务进度-1`
- 任务进度-2 `任务进度-2`

**消费者**:
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`

**下游写入**:
- → `任务分解与分配` (任务进度-1)
- → `任务分解与分配` (任务进度-2)
- → `研发项目` (每日报工跟新任务进度)

---

*自动生成于 赫立-研发 | 覆盖率: 2/20 核心实体 | 按 activity >= 4 筛选*