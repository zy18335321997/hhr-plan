# 赫立-研发 全局业务拓扑

> 从 dependency_graph.json 自动生成。 27 表 / 57 边 / 34 工作流。
> 只读表: 7 | 只写表: 7 | 双向: 3 | 孤立: 10

---

## 核心实体角色速查

| 实体 | r/w | 角色 | 核心工作流 |
|------|-----|------|-----------|
| **研发项目** | 6/3 | 流程节点 | 售后维修-研发处理、售后维修问题修复、提交审批、正式立项 |
| **任务分解与分配** | 5/2 | 数据生产者 | 人工成本新增至成本清单、任务进度-1、任务进度-2、测试不通过 |
| **项目WBS模板** | 1/3 | 流程节点 | 新增WBS任务 |

---

## 各实体生命周期明细

### 研发项目 (6写/3读)

**创建/更新者**:
- 售后维修-研发处理 `售后维修-研发处理`
- 售后维修问题修复 `售后维修问题修复`
- 提交审批 `提交审批`
- 正式立项 `正式立项`
- 每日报工跟新任务进度 `每日报工跟新任务进度`
- 研发维修BUG审批 `研发维修BUG审批`

**消费者**:
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`
- 自动生成薪资明细 `自动生成薪资明细`

**下游写入**:
- → `产品/服务` (获取BOM模板)
- → `项目WBS模板` (新增WBS任务)
- → 链式影响: `员工组织职级`

### 任务分解与分配 (5写/2读)

**创建/更新者**:
- 人工成本新增至成本清单 `人工成本新增至成本清单`
- 任务进度-1 `任务进度-1`
- 任务进度-2 `任务进度-2`
- 测试不通过 `测试不通过`
- 测试通过 `测试通过`

**消费者**:
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`

**下游写入**:
- → `研发项目` (每日报工跟新任务进度)
- → `研发项目` (提交审批)
- → `任务分解与分配` (任务进度-1)
- → `任务分解与分配` (任务进度-2)

### 项目WBS模板 (1写/3读)

**创建/更新者**:
- 新增WBS任务 `新增WBS任务`

**消费者**:
- 新增WBS任务 `新增WBS任务`
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`

**下游写入**:

---

*自动生成于 赫立-研发 | 覆盖率: 3/23 核心实体 | 按 activity >= 4 筛选*