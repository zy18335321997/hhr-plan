# 赫立-研发管理 全局业务拓扑

> 从 dependency_graph.json 自动生成。 16 表 / 40 边 / 21 工作流。
> 只读表: 5 | 只写表: 2 | 双向: 3 | 孤立: 6

---

## 核心实体角色速查

| 实体 | r/w | 角色 | 核心工作流 |
|------|-----|------|-----------|
| **任务分解与分配** | 6/0 | 数据生产者 | 任务进度-1、任务进度-2、新增WBS任务、新增分配任务模板 |
| **BOM（物料清单）** | 2/2 | 辅助表 | 递归，获取bom模板资料-1、递归，获取bom模板资料-2 |
| **研发项目** | 1/3 | 流程节点 | 每日报工跟新任务进度 |

---

## 各实体生命周期明细

### 任务分解与分配 (6写/0读)

**创建/更新者**:
- 任务进度-1 `任务进度-1`
- 任务进度-2 `任务进度-2`
- 新增WBS任务 `新增WBS任务`
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`
- 每日报工 `每日报工`



### BOM（物料清单） (2写/2读)

**创建/更新者**:
- 递归，获取bom模板资料-1 `递归，获取bom模板资料-1`
- 递归，获取bom模板资料-2 `递归，获取bom模板资料-2`

**消费者**:
- 递归，获取bom模板资料-1 `递归，获取bom模板资料-1`
- 递归，获取bom模板资料-2 `递归，获取bom模板资料-2`

**下游写入**:

### 研发项目 (1写/3读)

**创建/更新者**:
- 每日报工跟新任务进度 `每日报工跟新任务进度`

**消费者**:
- 新增分配任务模板 `新增分配任务模板`
- 新增分配任务模板-2 `新增分配任务模板-2`
- 自动生成薪资明细 `自动生成薪资明细`

**下游写入**:
- → 链式影响: `任务分解与分配`, `薪资发放明细`

---

*自动生成于 赫立-研发管理 | 覆盖率: 3/15 核心实体 | 按 activity >= 4 筛选*