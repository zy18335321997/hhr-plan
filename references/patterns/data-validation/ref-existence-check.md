# 关联记录存在性校验

## 适用场景
- 业务动作依赖供应商、合同、项目等关联记录。
- 关联记录可能被删除、停用或尚未创建。

## 节点链
`触发 → 获取单条(按记录ID) → 分支(存在?) → 是:继续 / 否:标记异常+通知`

## 设计要求
- 查询必须使用关联记录 ID，不用名称模糊匹配。
- 空结果不得继续执行依赖该记录的写操作。
- 失败路径记录来源实体、关联字段和恢复入口。
- `design_ir.traceability` 关联到对应需求和业务规则。
