# 数量与金额范围校验

## 适用场景
- 金额、数量、折扣或时长存在上下限。
- 不同角色或业务类型使用不同阈值。

## 节点链
`触发 → 读取阈值 → 分支(低于/正常/高于) → 正常:继续 / 越界:标记+通知`

## 设计要求
- 阈值来源必须可追溯，禁止把业务阈值只写在节点描述中。
- 覆盖空值、边界值和单位换算。
- 越界是否阻断由客户规则决定，不默认“通知后继续”。
- 记录命中规则、原值、阈值和处理人。
