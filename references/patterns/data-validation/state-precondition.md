# 状态前置条件

## 适用场景
- 按钮或自动流程只能在指定状态执行。
- 棕地修改需要防止并发状态变化。

## 节点链
`触发 → 获取当前记录 → 分支(状态+版本匹配?) → 是:执行 / 否:停止+提示刷新`

## 设计要求
- `design_ir.state_machines` 必须存在对应转换。
- 同时校验状态和版本号/更新时间，避免读取后被并发修改。
- 失败不得写入目标字段；提示当前状态、预期状态和重新操作入口。
- 成功后同步写状态、时间、操作人和审计记录。
