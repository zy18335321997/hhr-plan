# 几建 全局业务拓扑

> 从 dependency_graph.json 自动生成。 164 表 / 1076 边 / 338 工作流。
> 只读表: 23 | 只写表: 36 | 双向: 27 | 孤立: 78

---

## 核心实体角色速查

| 实体 | r/w | 角色 | 核心工作流 |
|------|-----|------|-----------|
| **项目 实施** | 46/11 | 数据生产者 | 任务完成项目经理审批、任务挂起申请审批、任务时间延期、任务确认-修改 |
| **任务清单** | 20/12 | 业务Hub | 修改我的采购申请、修改我的采购申请内容，自动同步通、发布任务、发布任务明细（任务流转新增） |
| **入库申请** | 5/14 | 数据消费者 | 新增采购合同、根据入库逐条新增流水与库存、根据采购入库（入库即出库）更新仓、生成维修出库单 |
| **库存信息表** | 7/12 | 业务Hub | 借用申请新增出库明细、占用数量跟新、新增占用数量、新增固定资产-若为新购设备 |
| **货品信息** | 11/8 | 业务Hub | A、入库明细更新物品单价、成品出库》出库申请明细、新增入库明细 |
| **实施项目明细** | 3/13 | 数据消费者 | 品检不合格、完成任务、采购任务 |
| **出库申请** | 3/11 | 数据消费者 | 仓库签收、写入项目、根据出库单逐条更新库存与流水 |
| **仓库信息** | 2/11 | 数据消费者 | 新增库存调拨、根据调拨单逐条处理货品 |
| **产品档案** | 6/4 | 流程节点 | A、出库清单新增自动新增出库申请明细、新增入库明细、流转》品检完成之后自动生产成成品 |
| **入库明细（入库）** | 1/9 | 数据消费者 | 根据出库单逐条更新库存与流水 |
| **科目预算** | 6/4 | 流程节点 | A、新增个人报销、新增对公转账、新增现金支付 |
| **付款计划** | 0/9 | 数据消费者 | 取消采购-作废采购入库申请、完成支付、对账明细子流程 |
| **询价明细（采购需求）** | 5/4 | 流程节点 | 修改我的采购申请内容，自动同步通、根据入库逐条新增流水与库存、根据出库单逐条更新库存与流水、生成采购合同 |
| **采购合同** | 5/4 | 流程节点 | 对账明细子流程、新增对公转账、新增对公转账多条、根据入库逐条新增流水与库存 |
| **采购物品明细（采购合同）** | 0/9 | 数据消费者 | 取消采购-作废采购入库申请、对账明细子流程、新增对公转账 |
| **销售合同订单** | 4/5 | 流程节点 | 回款审批、新增开票、新增开票-子流程、转商机 |
| **出货清单** | 1/7 | 数据消费者 | 完成任务 |
| **采购需求** | 4/4 | 流程节点 | 新增时自动触发流转审批、生成采购合同、采用、采购任务 |
| **供应商信息** | 5/2 | 数据生产者 | 对账明细子流程、批量合并新增对公转账、新增对公转账、新增对公转账单条 |
| **售前** | 7/0 | 数据生产者 | 产品报价、任务完成项目经理审批、完成任务、报价单审核 |

---

## 各实体生命周期明细

### 项目 实施 (46写/11读)

**创建/更新者**:
- 任务完成项目经理审批 `任务完成项目经理审批`
- 任务挂起申请审批 `任务挂起申请审批`
- 任务时间延期 `任务时间延期`
- 任务确认-修改 `任务确认-修改`
- 任务确认-新增 `任务确认-新增`
- 任务转派请求 `任务转派请求`
- 修改我的采购申请 `修改我的采购申请`
- 修改我的采购申请内容，自动同步通 `修改我的采购申请内容，自动同步通`

**消费者**:
- 完成任务 `完成任务`
- 提交审批 `提交审批`
- 新增时自动触发流转审批 `新增时自动触发流转审批`
- 每日更新售前阶段商机跟进状态 `每日更新售前阶段商机跟进状态`
- 生产领料 `生产领料`
- 直接新增采购需求 `直接新增采购需求`
- 获取项目内产品明细 `获取项目内产品明细`
- 转商机 `转商机`
- 逐个处理模版 `逐个处理模版`
- 逐条新增任务清单 `逐条新增任务清单`

**下游写入**:
- → `成本预算子表` (分包采购跟新成本)
- → `从工作表获取` (逐个商机找寻项目并汇总提成)
- → `成本预算子表` (逐个商机找寻项目并汇总提成)
- → `任务清单` (发布任务)
- → 链式影响: `产品报价单`, `任务清单`, `客户`, `所属任务`, `采购需求`, `销售合同订单`

### 任务清单 (20写/12读)

**创建/更新者**:
- 修改我的采购申请 `修改我的采购申请`
- 修改我的采购申请内容，自动同步通 `修改我的采购申请内容，自动同步通`
- 发布任务 `发布任务`
- 发布任务明细（任务流转新增） `发布任务明细（任务流转新增）`
- 子任务：完成任务 `子任务：完成任务`
- 完成任务 `完成任务`
- 建立方案 `建立方案`
- 提交审批 `提交审批`

**消费者**:
- 修改我的采购申请内容，自动同步通 `修改我的采购申请内容，自动同步通`
- 出库申请 `出库申请`
- 创建任务清单 `创建任务清单`
- 品检不合格 `品检不合格`
- 售前商机新增修改附件时自动新增到 `售前商机新增修改附件时自动新增到`
- 建立方案 `建立方案`
- 确认收货 `确认收货`
- 资料传递 `资料传递`
- 输出附件=新增任务清单的附件（参 `输出附件=新增任务清单的附件（参`
- 逐个处理模版 `逐个处理模版`

**下游写入**:
- → `售前` (任务完成项目经理审批)
- → `任务流转` (发布任务)
- → `项目 实施` (任务完成项目经理审批)
- → `出货清单` (完成任务)
- → `售前` (完成任务)
- → 链式影响: `实施项目明细`, `新增记录`, `询价明细（采购需求）`, `项目 实施`

### 入库申请 (5写/14读)

**创建/更新者**:
- 新增采购合同 `新增采购合同`
- 根据入库逐条新增流水与库存 `根据入库逐条新增流水与库存`
- 根据采购入库（入库即出库）更新仓 `根据采购入库（入库即出库）更新仓`
- 生成维修出库单 `生成维修出库单`
- 采购合同取消后入库材料退料 `采购合同取消后入库材料退料`

**消费者**:
- 入库申请明细新增 `入库申请明细新增`
- 出库 `出库`
- 取消采购-作废采购入库申请 `取消采购-作废采购入库申请`
- 成品出库 `成品出库`
- 成品单独生成编号 `成品单独生成编号`
- 挂单 `挂单`
- 未命名子流程 `未命名子流程`
- 流转》品检完成之后自动生产成成品 `流转》品检完成之后自动生产成成品`
- 继续发货 `继续发货`
- 调拨入库拆分成品编码 `调拨入库拆分成品编码`

**下游写入**:
- → `新增记录` (采购合同取消修改未入库的状态)
- → `项目 实施` (入库申请新)
- → `供应商询价表` (入库申请（按钮）)
- → 链式影响: `产品档案`, `借用明细`, `新增记录`, `货品信息`, `项目 实施`

### 库存信息表 (7写/12读)

**创建/更新者**:
- 借用申请新增出库明细 `借用申请新增出库明细`
- 占用数量跟新 `占用数量跟新`
- 新增占用数量 `新增占用数量`
- 新增固定资产-若为新购设备 `新增固定资产-若为新购设备`
- 未命名子流程 `未命名子流程`
- 根据调拨单逐条处理货品 `根据调拨单逐条处理货品`
- 逐条更新库存信息表的可用库存 `逐条更新库存信息表的可用库存`

**消费者**:
- 出库明细子 `出库明细子`
- 出库清单新增自动新增出库申请明细 `出库清单新增自动新增出库申请明细`
- 成品出库》出库申请明细 `成品出库》出库申请明细`
- 未命名子流程 `未命名子流程`
- 根据入库逐条新增流水与库存 `根据入库逐条新增流水与库存`
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`
- 根据调拨单逐条处理货品 `根据调拨单逐条处理货品`
- 根据采购入库（入库即出库）更新仓 `根据采购入库（入库即出库）更新仓`
- 生成维修出库明细 `生成维修出库明细`
- 调拨入库拆分成品编码 `调拨入库拆分成品编码`

**下游写入**:
- → 链式影响: `产品档案`, `从多条数据中获取`, `从工作表获取`, `仓库信息`, `仓库调拨`, `任务清单`, `入库明细（入库）`, `入库申请` ...

### 货品信息 (11写/8读)

**创建/更新者**:
- A `A`
- 入库明细更新物品单价 `入库明细更新物品单价`
- 成品出库》出库申请明细 `成品出库》出库申请明细`
- 新增入库明细 `新增入库明细`
- 根据入库逐条新增流水与库存 `根据入库逐条新增流水与库存`
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`
- 根据调拨单逐条处理货品 `根据调拨单逐条处理货品`
- 根据采购入库（入库即出库）更新仓 `根据采购入库（入库即出库）更新仓`

**消费者**:
- 产品新增自动触发匹配货品信息 `产品新增自动触发匹配货品信息`
- 入库 `入库`
- 出库清单新增自动新增出库申请明细 `出库清单新增自动新增出库申请明细`
- 流转》品检完成之后自动生产成成品 `流转》品检完成之后自动生产成成品`
- 货品信息去重 `货品信息去重`
- 跟新实施项目明细（通过合同） `跟新实施项目明细（通过合同）`
- 跟新实施项目明细（通过报价单） `跟新实施项目明细（通过报价单）`
- 采购货品明细（采购合同） `采购货品明细（采购合同）`

**下游写入**:
- → 链式影响: `产品档案`, `项目 实施`

### 实施项目明细 (3写/13读)

**创建/更新者**:
- 品检不合格 `品检不合格`
- 完成任务 `完成任务`
- 采购任务 `采购任务`

**消费者**:
- 任务确认-修改 `任务确认-修改`
- 任务确认-新增 `任务确认-新增`
- 免检入库 `免检入库`
- 入库 `入库`
- 单条项目产品明细执行 `单条项目产品明细执行`
- 发布任务 `发布任务`
- 成品出库》出库申请明细 `成品出库》出库申请明细`
- 成品单独生成编号 `成品单独生成编号`
- 提交审批 `提交审批`
- 新增出库清单自动新增出库申请 `新增出库清单自动新增出库申请`

**下游写入**:
- → `产品档案` (直接新增时判断货品信息是否存在)
- → `产品档案` (出库清单新增自动新增出库申请明细)
- → `项目 实施` (出库)
- → `产品档案` (新增入库明细)
- → `货品信息` (新增入库明细)
- → 链式影响: `从多条数据中获取`, `从工作表获取`, `任务清单`, `入库明细（入库）`, `入库申请`, `出库申请`, `新增记录`, `询价明细（采购需求）` ...

### 出库申请 (3写/11读)

**创建/更新者**:
- 仓库签收 `仓库签收`
- 写入项目 `写入项目`
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`

**消费者**:
- 借用申请新增出库明细 `借用申请新增出库明细`
- 出库明细子 `出库明细子`
- 出库清单新增自动新增出库申请明细 `出库清单新增自动新增出库申请明细`
- 成品出库》出库申请明细 `成品出库》出库申请明细`
- 未命名子流程 `未命名子流程`
- 根据采购入库（入库即出库）更新仓 `根据采购入库（入库即出库）更新仓`
- 生产退料 `生产退料`
- 生成维修出库明细 `生成维修出库明细`
- 返厂维修入库 `返厂维修入库`
- 采购合同取消后入库材料退料 `采购合同取消后入库材料退料`

**下游写入**:
- → `借用记录` (出库申请新)
- → `项目 实施` (出库申请新)
- → 链式影响: `产品档案`, `从多条数据中获取`, `入库申请`, `库存信息表`, `询价明细（采购需求）`, `货品信息`

### 仓库信息 (2写/11读)

**创建/更新者**:
- 新增库存调拨 `新增库存调拨`
- 根据调拨单逐条处理货品 `根据调拨单逐条处理货品`

**消费者**:
- 仓库签收 `仓库签收`
- 供应商维修 `供应商维修`
- 免检入库 `免检入库`
- 入库 `入库`
- 入库寄回 `入库寄回`
- 出库申请新 `出库申请新`
- 提交审批 `提交审批`
- 新增出库清单自动新增出库申请 `新增出库清单自动新增出库申请`
- 新增时自动触发流转审批 `新增时自动触发流转审批`
- 新增采购合同 `新增采购合同`

**下游写入**:
- → 链式影响: `任务清单`, `入库申请`, `出库申请`, `成本预算子表`, `科目预算`, `通用模板（新增流转任务）`, `采购需求`, `项目 实施`

### 产品档案 (6写/4读)

**创建/更新者**:
- A `A`
- 出库清单新增自动新增出库申请明细 `出库清单新增自动新增出库申请明细`
- 新增入库明细 `新增入库明细`
- 流转》品检完成之后自动生产成成品 `流转》品检完成之后自动生产成成品`
- 直接新增时判断货品信息是否存在 `直接新增时判断货品信息是否存在`
- 跟新实施项目明细（通过合同） `跟新实施项目明细（通过合同）`

**消费者**:
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`
- 跟新产品编号 `跟新产品编号`
- 跟新实施项目明细（通过合同） `跟新实施项目明细（通过合同）`
- 跟新实施项目明细（通过报价单） `跟新实施项目明细（通过报价单）`

**下游写入**:
- → 链式影响: `从工作表获取`, `入库明细（入库）`, `出库申请`, `询价明细（采购需求）`, `货品信息`

### 入库明细（入库） (1写/9读)

**创建/更新者**:
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`

**消费者**:
- 入库申请新 `入库申请新`
- 入库申请新-子流程 `入库申请新-子流程`
- 出库申请 `出库申请`
- 成品单独生成编号 `成品单独生成编号`
- 生成维修出库单 `生成维修出库单`
- 调拨入库拆分成品编码 `调拨入库拆分成品编码`
- 追加材料子流程 `追加材料子流程`
- 采购合同取消修改未入库的状态 `采购合同取消修改未入库的状态`
- 项目产品明细-出库明细 `项目产品明细-出库明细`

**下游写入**:
- → `入库申请` (根据采购入库（入库即出库）更新仓库流水)
- → `货品信息` (根据采购入库（入库即出库）更新仓库流水)
- → `货品信息` (入库明细更新物品单价)
- → `从工作表获取` (根据入库逐条新增流水与库存)
- → `任务清单` (根据入库逐条新增流水与库存)
- → 链式影响: `入库申请`, `新增记录`, `通用模板（新增流转任务）`, `项目 实施`

### 科目预算 (6写/4读)

**创建/更新者**:
- A `A`
- 新增个人报销 `新增个人报销`
- 新增对公转账 `新增对公转账`
- 新增现金支付 `新增现金支付`
- 新增采购合同 `新增采购合同`
- 新增采购（无合同）审批 `新增采购（无合同）审批`

**消费者**:
- 下个月预计开支情况 `下个月预计开支情况`
- 新增提成发放记录时更新提成表与预 `新增提成发放记录时更新提成表与预`
- 新增财务月报 `新增财务月报`
- 逐个商机找寻项目并汇总提成 `逐个商机找寻项目并汇总提成`

**下游写入**:
- → 链式影响: `从工作表获取`, `成本预算子表`, `项目提成表`

### 付款计划 (0写/9读)

**创建/更新者**: — (表单直接编辑或外部系统)

**消费者**:
- 取消采购-作废采购入库申请 `取消采购-作废采购入库申请`
- 完成支付 `完成支付`
- 对账明细子流程 `对账明细子流程`
- 挂单 `挂单`
- 新增对公转账 `新增对公转账`
- 新增对公转账单条 `新增对公转账单条`
- 新增对公转账多条 `新增对公转账多条`
- 新增财务月报 `新增财务月报`
- 继续发货 `继续发货`

**下游写入**:
- → `供应商信息` (新增对公转账)
- → `采购合同` (新增对公转账)
- → `供应商信息` (新增对公转账多条)
- → `采购合同` (新增对公转账多条)
- → `供应商信息` (批量合并新增对公转账)
- → 链式影响: `供应商信息`, `外协物流`, `成本预算子表`, `科目预算`, `采购合同`, `项目 实施`

### 询价明细（采购需求） (5写/4读)

**创建/更新者**:
- 修改我的采购申请内容，自动同步通 `修改我的采购申请内容，自动同步通`
- 根据入库逐条新增流水与库存 `根据入库逐条新增流水与库存`
- 根据出库单逐条更新库存与流水 `根据出库单逐条更新库存与流水`
- 生成采购合同 `生成采购合同`
- 采购合同取消后入库材料退料 `采购合同取消后入库材料退料`

**消费者**:
- 新增时自动触发流转审批 `新增时自动触发流转审批`
- 生成询价表 `生成询价表`
- 获取询价链接 `获取询价链接`
- 采购任务 `采购任务`

**下游写入**:
- → `货品信息` (采购需求新增之后创建实施明细)
- → `项目 实施` (采购需求新增之后创建实施明细)
- → `库存信息表` (新增占用数量)
- → `库存信息表` (占用数量跟新)
- → `供应商询价` (新增询价单明细)
- → 链式影响: `任务清单`, `实施项目明细`, `查找采购的任务清单`, `采购需求`, `项目 实施`

### 采购合同 (5写/4读)

**创建/更新者**:
- 对账明细子流程 `对账明细子流程`
- 新增对公转账 `新增对公转账`
- 新增对公转账多条 `新增对公转账多条`
- 根据入库逐条新增流水与库存 `根据入库逐条新增流水与库存`
- 生成采购合同 `生成采购合同`

**消费者**:
- 入库申请（按钮） `入库申请（按钮）`
- 新增采购合同 `新增采购合同`
- 追加材料 `追加材料`
- 采购合同通过后检查项目阶段 `采购合同通过后检查项目阶段`

**下游写入**:
- → `任务清单` (新增采购合同)
- → `入库申请` (新增采购合同)
- → `成本预算子表` (新增采购合同)
- → `科目预算` (新增采购合同)
- → `项目 实施` (新增采购合同)
- → 链式影响: `任务清单`, `供应商询价表`, `入库申请`, `成本预算子表`, `科目预算`, `项目 实施`

### 采购物品明细（采购合同） (0写/9读)

**创建/更新者**: — (表单直接编辑或外部系统)

**消费者**:
- 取消采购-作废采购入库申请 `取消采购-作废采购入库申请`
- 对账明细子流程 `对账明细子流程`
- 新增对公转账 `新增对公转账`
- 新增对公转账多条 `新增对公转账多条`
- 新增询价单明细 `新增询价单明细`
- 新增采购合同 `新增采购合同`
- 生成采购合同 `生成采购合同`
- 追加材料 `追加材料`
- 重新建立采购合同 `重新建立采购合同`

**下游写入**:
- → 链式影响: `任务清单`, `供应商信息`, `供应商询价`, `入库申请`, `成本预算子表`, `新增记录`, `科目预算`, `询价明细（采购需求）` ...

### 销售合同订单 (4写/5读)

**创建/更新者**:
- 回款审批 `回款审批`
- 新增开票 `新增开票`
- 新增开票-子流程 `新增开票-子流程`
- 转商机 `转商机`

**消费者**:
- 按月新增回款A `按月新增回款A`
- 按月新增回款B `按月新增回款B`
- 新增销售合同 `新增销售合同`
- 跟新实施项目明细（通过合同） `跟新实施项目明细（通过合同）`
- 转商机 `转商机`

**下游写入**:
- → `项目 实施` (新增销售合同)
- → 链式影响: `产品报价单`, `产品档案`, `客户`, `项目 实施`

### 出货清单 (1写/7读)

**创建/更新者**:
- 完成任务 `完成任务`

**消费者**:
- 供应商维修 `供应商维修`
- 入库寄回 `入库寄回`
- 出库申请新 `出库申请新`
- 客户签收 `客户签收`
- 换货出库 `换货出库`
- 生成维修出库单 `生成维修出库单`
- 通用模板（新增流转任务） `通用模板（新增流转任务）`

**下游写入**:
- → `通用模板（新增流转任务）` (新增出库清单自动新增出库申请)
- → `项目 实施` (确认收货)
- → 链式影响: `借用记录`, `入库申请`, `新增记录`, `通用模板（新增流转任务）`, `项目 实施`

### 采购需求 (4写/4读)

**创建/更新者**:
- 新增时自动触发流转审批 `新增时自动触发流转审批`
- 生成采购合同 `生成采购合同`
- 采用 `采用`
- 采购任务 `采购任务`

**消费者**:
- 新增时自动触发流转审批 `新增时自动触发流转审批`
- 新增询价单明细 `新增询价单明细`
- 获取询价链接 `获取询价链接`
- 采购需求新增之后创建实施明细 `采购需求新增之后创建实施明细`

**下游写入**:
- → `任务清单` (生成询价表)
- → `任务清单` (直接新增采购需求)
- → `项目 实施` (直接新增采购需求)
- → 链式影响: `任务清单`, `供应商询价`, `新增记录`, `货品信息`, `项目 实施`

### 供应商信息 (5写/2读)

**创建/更新者**:
- 对账明细子流程 `对账明细子流程`
- 批量合并新增对公转账 `批量合并新增对公转账`
- 新增对公转账 `新增对公转账`
- 新增对公转账单条 `新增对公转账单条`
- 新增对公转账多条 `新增对公转账多条`

**消费者**:
- 未命名子流程 `未命名子流程`
- 生成询价表 `生成询价表`

**下游写入**:
- → `任务清单` (获取询价链接)
- → 链式影响: `任务清单`

### 售前 (7写/0读)

**创建/更新者**:
- 产品报价 `产品报价`
- 任务完成项目经理审批 `任务完成项目经理审批`
- 完成任务 `完成任务`
- 报价单审核 `报价单审核`
- 报价单归档 `报价单归档`
- 负责人跟新，拥有者也跟新 `负责人跟新，拥有者也跟新`
- 销售合同 `销售合同`


**下游写入**:
- → `客户` (项目新增建立客户基础信息)
- → `产品报价单` (转商机)
- → `客户` (转商机)
- → `销售合同订单` (转商机)
- → `任务清单` (建立方案)

---

*自动生成于 几建 | 覆盖率: 20/155 核心实体 | 按 activity >= 4 筛选*